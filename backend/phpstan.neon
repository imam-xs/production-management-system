includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6

    paths:
        - app
        - database
        - routes
        - tests

    # No baseline and no ignored errors — level 6 passes cleanly. If an
    # exception ever becomes necessary, add it here with a comment explaining
    # why, rather than lowering the level.
